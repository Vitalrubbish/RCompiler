fn getElement(arr: &mut[i32; 4], a : usize) -> i32 {
    arr[a]
}

fn main() {
    let mut arr : [i32; 4] = [1, 2, 3, 4];
    let mut a : usize = 3;
    printlnInt(getElement(&mut arr, a));
    exit(0);
}